# Edu CoreX App Releases

This branch starts the packaged app archive from version 0.0.0.

Large binaries such as APK, EXE, IPA, blockmaps, and signed installers should be attached to GitHub Releases instead of being committed directly to git. The release folders in this branch keep small manifests with artifact names, paths, sizes, and hashes.

